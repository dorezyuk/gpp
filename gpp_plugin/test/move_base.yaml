base_global_planner: "gpp_plugin::GppPlugin"

# note: move_base assumes that the parameters live under the "pluginlib-name"
# of the loaded planner
GppPlugin:
  planning:
    - {name: global_planner,      type: global_planner/GlobalPlanner}
